<%= form_for(current_user.active_relationships.find_by(tracked_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Untrack", class: "btn btn-warning" %>
<% end %>